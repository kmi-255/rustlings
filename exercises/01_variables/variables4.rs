// variables4.rs
//
// Execute `rustlings hint variables4` or use the `hint` watch subcommand for a
// hint.



fn main() {
    let mut x = 3; // Michaël : Ajout de la mutability pour la variable x pour qu'elle puisse être changée plus tard dans le code
    println!("Number {}", x);
    x = 5; // don't change this line
    println!("Number {}", x);
}
